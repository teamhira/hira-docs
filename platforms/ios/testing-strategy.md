# iOS Testing Strategy

## Testing Types
- **Unit Tests**: Mandatory for domain logic and ViewModels.
- **UI Tests**: Required for critical user flows (Login, Quran navigation).
- **Snapshot Tests**: Used for verifying UI components consistency.

## Minimum Gate
New features must include at least core unit tests before they can be merged.
