# Web Coding Standards

## Primary Standards
- **Component Pattern**: Separate presentational (UI) and container (logic) components.
- **Service Layer**: Use a centralized service layer as the single entry point for API communication (GraphQL).
- **Utility Reuse**: Prevent utility duplication across different feature modules.
